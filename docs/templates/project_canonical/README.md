# Canonical Project Template

Copy this folder to:

```text
docs/projects/<system>/<project_id>/
```

Keep:

- numbered spine docs in the project root
- active rules in `contracts/`
- historical material in `raw/`

Do not promote a project to `bot_usage: allowed` until the validation and
promotion contracts pass.
